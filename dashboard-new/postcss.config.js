// Local PostCSS config for dashboard-new
// Tailwind v4 does not require the tailwindcss PostCSS plugin.
// Keep only autoprefixer to avoid conflicts with the root config.
export default {
  plugins: {
    autoprefixer: {},
  },
};

